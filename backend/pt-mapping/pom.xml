<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.precise.test</groupId>
        <artifactId>precise-test-platform</artifactId>
        <version>0.1.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>pt-mapping</artifactId>
    <name>pt-mapping</name>
    <description>用例-代码关联模块（占位）：关联关系实体 + Service 接口签名</description>

    <dependencies>
        <dependency>
            <groupId>com.precise.test</groupId>
            <artifactId>pt-common</artifactId>
        </dependency>
        <!-- M4：读取接口定义（controllerClass/controllerMethod）与用例，建立关联 -->
        <dependency>
            <groupId>com.precise.test</groupId>
            <artifactId>pt-analyze</artifactId>
        </dependency>
        <dependency>
            <groupId>com.precise.test</groupId>
            <artifactId>pt-casegen</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
    </dependencies>
</project>
